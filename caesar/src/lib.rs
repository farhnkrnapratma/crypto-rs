pub fn encrypt(plaintext: &str) -> &str {
    plaintext
}

pub fn decrypt(ciphertext: &str) -> &str {
    ciphertext
}

/*#[cfg(test)]
mod tests {
  use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}*/
